# GitHub Copilot — repository instructions for <<PROJECT_NAME>>

> Derived from `PROJECT_RULES.md`. Regenerate after rule changes.

## Read first

1. `PROJECT_MAP.md`
2. `STATE.md`
3. `LESSONS.md` — skim relevant categories; carry active rules as constraints.
4. `PROJECT_RULES.md`
5. `PRODUCT.md`
6. Topic-specific: `CODING_RULES.md` / `DESIGN.md` / `DATABASE.md` / `DEPLOYMENT.md`.

## Stack

- Frontend: <<FRONTEND_STACK>>
- Backend: <<BACKEND_STACK>>
- Database: <<DATABASE_STACK>>
- Auth: <<AUTH_STACK>>
- Hosting: <<HOSTING_STACK>>

No new dependencies without explicit approval.

## Language policy

- Markdown: <<DOCS_LANGUAGE>>
- Code comments: <<CODE_COMMENT_LANGUAGE>>
- UI copy: <<UI_LANGUAGE>>
- Commit messages: <<COMMIT_LANGUAGE>>

## Must-do for any change

- Doc block on every exported function (<<CODE_COMMENT_LANGUAGE>>).
- Loading / empty / error states on every screen.
- Schema changes via migration file only.
- Same-diff updates to `STATE.md`, `CHANGELOG.md` (append), `CLIENT_DOCS.md` (when client-visible).
- Never commit secrets — use `.env` + `process.env` only.
- After ANY correction from the user: acknowledge, fix, then append a lesson to `LESSONS.md` (pattern + root cause + preventive rule) BEFORE continuing.
- Validate all user input at system boundaries. Parameterised queries only.
<<#IF_TDD_BASIC>>
- Write at least one happy-path test per new function before marking done.
<</#IF_TDD_BASIC>>
<<#IF_TDD_FULL>>
- **Full TDD:** write failing test first (RED) → implement (GREEN) → refactor. Coverage ≥ 80%.
<</#IF_TDD_FULL>>

## Must-not-do

- Destructive shell ops without confirmation.
- Edit past CHANGELOG entries.
- Introduce a new framework/library without approval.
- Touch `templates/` (should be deleted after the interview).
- Hardcode secrets, API keys, or tokens anywhere in source files.
- Commit `.env` files (`.env.example` only).

## Implementation guidelines (immutable)

> **Do not edit this section by hand.** Source: [andrej-karpathy-skills].

### 1. Think Before Coding

State assumptions; present interpretations; suggest simpler approaches; stop and ask when unclear.

### 2. Simplicity First

Minimum code for the request only — no speculative features, abstractions, or unrequested flexibility.

### 3. Surgical Changes

Change only what the request requires; match existing style; don't refactor unrelated code; remove only orphans your edit created.

### 4. Goal-Driven Execution

Define verifiable success criteria; use test-first goals for bugs and validation; state a brief plan with verify steps for multi-step work.

## When unsure

Ask one targeted question with 3-4 candidate answers and wait.
