# Lovable Project Knowledge — paste-ready block for <<PROJECT_NAME>>

> This file is **paste-ready** for the Lovable "Project Knowledge" field. Copy the block between the markers below into Lovable. Re-paste after any change to `PROJECT_RULES.md`.

---

<<<COPY_FROM_HERE>>>

You are working on **<<PROJECT_NAME>>** — <<PROJECT_ONE_LINER>>.

The repository contains canonical documentation. Read these before any task, in this order:

1. PROJECT_MAP.md — file/module map.
2. STATE.md — what currently exists.
3. LESSONS.md — skim categories relevant to current task; carry active rules as constraints.
4. PROJECT_RULES.md — binding rules for any agent.
5. PRODUCT.md — what the product is supposed to do.
6. Topic-specific:
   - CODING_RULES.md for code edits.
   - DESIGN.md for UI work.
   - DATABASE.md for schema work.
   - DEPLOYMENT.md for infra / env work.

If the answer is not in those files, ask the user one targeted question with 3-4 candidate answers.

Stack baseline (do not change without approval):
- Frontend: <<FRONTEND_STACK>>
- Backend: <<BACKEND_STACK>>
- Database: <<DATABASE_STACK>>
- Auth: <<AUTH_STACK>>
- Storage: <<STORAGE_STACK>>
- Hosting: <<HOSTING_STACK>>

Language policy:
- Markdown docs: <<DOCS_LANGUAGE>>
- Code comments / JSDoc: <<CODE_COMMENT_LANGUAGE>>
- UI copy: <<UI_LANGUAGE>>
- Commit messages: <<COMMIT_LANGUAGE>>

Non-negotiables:
- Doc block on every exported function, readable by a non-technical reader.
- Loading / empty / error states on every screen.
- Schema changes only via migration files.
- Update STATE.md, CHANGELOG.md (append-only), and CLIENT_DOCS.md (when client-visible) in the same change as the code.
- NEVER commit secrets or API keys; use env vars only. NEVER hardcode tokens in source.
- After ANY correction from the user: acknowledge, fix, then append a lesson to LESSONS.md (pattern + root cause + preventive rule) BEFORE continuing. See PROJECT_RULES.md §3.1.
- Validate all user input at system boundaries. Parameterised queries only — no string-concatenated SQL.
<<#IF_TDD_BASIC>>
- Write at least one happy-path test per new function before marking the feature done.
<</#IF_TDD_BASIC>>
<<#IF_TDD_FULL>>
- Full TDD: write failing test first (RED) → implement minimally (GREEN) → refactor. Target coverage ≥ 80%.
<</#IF_TDD_FULL>>

For `/atomic-prompts <idea>` requests, do NOT implement inline — instead produce `atomic-prompts/NNNN-<slug>.md` using the schema in `docs/PROMPT_AUTHORING_GUIDE.md` (or the locally installed skill file). Print the path and the count of acceptance criteria, then stop.

Implementation guidelines (immutable — from andrej-karpathy-skills; do not edit when pasting):
1. Think before coding: state assumptions; present interpretations; ask when unclear.
2. Simplicity first: minimum code for the request; no speculative features or abstractions.
3. Surgical changes: touch only what the request requires; match existing style; no drive-by refactors.
4. Goal-driven execution: verifiable success criteria; test-first for bugs/validation; plan with verify steps for multi-step work.

When in doubt, ask one targeted question with 3-4 candidate answers. Do not silently choose.

<<<COPY_TO_HERE>>>

---

## Pragmatic Lovable POC notes

If the project is currently in POC mode (`<<POC_OR_PROD>> == POC`):

- Doc rigour may be deferred to a "hardening pass" before going to production.
- Tests may be skipped for throwaway prototypes (must be noted in STATE.md).

Flip POC → PROD by editing `PROJECT_RULES.md` §13, regenerating this block, and re-pasting it into Lovable.
